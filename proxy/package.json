{
  "name": "guardrails-masking-proxy",
  "version": "1.0.0",
  "description": "Bedrock Guardrails (ApplyGuardrail) で MCP ツール結果の PII をマスクするプロキシ MCP サーバ",
  "type": "module",
  "bin": {
    "guardrails-masking-proxy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "smoke": "npm run build && node test/smoke.mjs"
  },
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.700.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "mcp-remote": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
